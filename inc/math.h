//
// Created by Wojcik98 on 14.10.18.
//

#ifndef RAZOR_MATH_H
#define RAZOR_MATH_H

#define abs(x) (((x) > 0) ? (x) : (-(x)))
#define sq(x) ((x) * (x))
#define min(x, y) (((x) < (y)) ? (x) : (y))
#define max(x, y) (((x) > (y)) ? (x) : (y))

#endif //RAZOR_MATH_H
